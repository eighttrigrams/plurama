{:id "001-initial-schema"
 :up ["CREATE TABLE IF NOT EXISTS users (
         id INTEGER PRIMARY KEY AUTOINCREMENT,
         name TEXT NOT NULL UNIQUE,
         created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
         updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
       )"
      "CREATE TABLE IF NOT EXISTS user_app_credentials (
         id INTEGER PRIMARY KEY AUTOINCREMENT,
         user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
         app TEXT NOT NULL,
         username TEXT NOT NULL,
         password TEXT NOT NULL,
         created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
         updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
         UNIQUE (user_id, app)
       )"
      "CREATE INDEX IF NOT EXISTS idx_user_app_credentials_user ON user_app_credentials(user_id)"]
 :down ["DROP INDEX IF EXISTS idx_user_app_credentials_user"
        "DROP TABLE IF EXISTS user_app_credentials"
        "DROP TABLE IF EXISTS users"]
 :transactions false}
